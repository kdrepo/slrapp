Your previous response was malformed.
Return ONLY a valid JSON array extracted from that response.
Each item must include keys: paperid, title, decision, reason.
Do not include markdown fences.

Original response:
{raw_response}
